version: '3.4'

services:
  client:
    image: client
    build:
      context: .
      dockerfile: ./Dockerfile
    volumes:
      - ./:/app
      - /app/node_modules
    environment:
      NODE_ENV: production
    ports:
      - 3000:3000
    # env_file:
    #   - ./.env
