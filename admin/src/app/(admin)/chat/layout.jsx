import Image from "next/image";
import Link from "next/link";
import React from "react";
export const metadata = {
  title: "Chat",
};
export default function LayoutChatRealTime({ children }) {
  return <div>{children}</div>;
}
